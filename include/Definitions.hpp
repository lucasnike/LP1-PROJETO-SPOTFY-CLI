#ifndef DEF_HPP
#define DEF_HPP

enum class MainMenu { musics = 1, playlists = 2, end = 3 };
enum class MusicsMenu { add = 1, remove = 2, list = 3, main = 4 };
enum class PlayListMenu { add, remove, list, main };
enum class PlayListMusicsMenu { add, remove, list, previus, main };


#endif